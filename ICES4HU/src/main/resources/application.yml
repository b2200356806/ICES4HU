spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ICES4HU_Jwt_Sec
    username: postgres
    password: Danish0987654321
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
